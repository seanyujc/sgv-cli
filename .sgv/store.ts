import { ActionContext } from "vuex";
import {
  // SGV-BUILD-STORE-IMPORT # NOT DELETE
} from "../../../common/core/store/mutationTypes";
import { Services } from "../../core/factory.serv";

interface I<%= uFKeyword%>State {
  // SGV-BUILD-STORE-INTERFACE # NOT DELETE
}

const xState: I<%= uFKeyword%>State = {
  // SGV-BUILD-STORE-STATE # NOT DELETE
};

const mutations = {
  // SGV-BUILD-STORE-MUTATIONS # NOT DELETE
};

const actions = {
  // SGV-BUILD-STORE-ACTIONS # NOT DELETE
};

const getters = {
  // SGV-BUILD-STORE-GETTERS # NOT DELETE
};

export default {
  actions,
  getters,
  mutations,
  namespaced: true,
  state: xState,
};
